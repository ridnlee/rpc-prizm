lua_package_path "/etc/nginx/lugate/?.lua;;";
#develop
        lua_code_cache off;
        #develop
server {
    set $service1 http://127.0.0.1:8882;
    set $service2 http://127.0.0.1:8883;
    set $default http://127.0.0.1:8884;

    listen          8881;

    location / {
        default_type 'application/json';
        access_by_lua_file /etc/nginx/lugate/main.lua;
    }

    location /serv1 {
        proxy_pass       $service1;
    }

    location /serv2 {
        proxy_pass       $service2;
    }

    location /default {
        proxy_pass       $default;
    }
}

# service01
server {
    listen       8882;

    access_log  /var/log/nginx/service01_access.log;
    error_log  /var/log/nginx/service01_error.log;

    location / {
          # MIME type determined by default_type:
          default_type 'application/json';

          content_by_lua_block {
             os.execute("sleep " .. tonumber(1))
             ngx.say('{"jsonrpc": "2.0", "result": [19, "server01", ' .. os.time() .. '], "id": 1}')
          }
    }
}
# service02
server {
    listen       8883;
    access_log  /var/log/nginx/service02_access.log;
    error_log  /var/log/nginx/service02_error.log;
    location / {
          # MIME type determined by default_type:
          default_type 'application/json';
          content_by_lua_block {
             ngx.say('{"jsonrpc": "2.0", "result": [-19, "server02", "' .. ngx.req.raw_header() .. '", "'.. ngx.var.request_uri ..'"], "id": 2}')
          }
    }
}